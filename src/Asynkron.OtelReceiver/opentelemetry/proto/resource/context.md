# `resource` Context

Resource descriptor schemas. Versioned content under [`v1/context.md`](v1/context.md) declares `Resource` and related
types used to annotate spans/logs/metrics with service metadata.
