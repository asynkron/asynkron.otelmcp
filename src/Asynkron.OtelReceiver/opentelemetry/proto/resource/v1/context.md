# `resource/v1` Context

- `resource.proto` declares OTLP `Resource` messages (attributes describing the producing service). Used heavily by
  translators and persistence (see [
  `../../../../TraceLens/Infra/Translator.cs`](../../../../TraceLens/Infra/Translator.cs)).
